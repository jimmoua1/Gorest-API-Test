package demo.notes.OOP;

public class MathMethods {
    int x = 1;
    int y = 2;

    // Math Methods
    int a = Math.min(x, y);
    int b = Math.max(x, y);
    double c = Math.sqrt(x);
    double d = Math.random();
    int e = Math.abs(x);
}
